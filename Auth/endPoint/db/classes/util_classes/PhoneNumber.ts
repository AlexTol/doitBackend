class PhoneNumber {
  countryCode: string;
  number: string;

  constructor(message: string) {
    //if match regex 1 do this...
    //if match regex n do this..
    //else throw error
  }
}

export { PhoneNumber };
